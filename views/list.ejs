<!DOCTYPE html>
<html lang="ko">
  <head>
    <%- include('./head') %>
    <link rel="stylesheet" href="/stylesheet/global.css" />
    <link rel="stylesheet" href="/stylesheet/frame.css" />
    <link rel="stylesheet" href="/stylesheet/list.css" />
  </head>
  <body>
    <div class="container">
      <div class="fixed-ratio">
        <div class="content">
          <div class="title">내가 만든 페이퍼</div>
          <div class="list-box">
            <% if(items) items.forEach(e=>{%>
            <div class="list-item" onclick="location.href='/paper/<%=e.id%>'">
              <div class="item-left">
                <%=e.title%><br />
                #<%=e.id%>
              </div>
              <div><%=String(e.message.length)%></div>
            </div>
            <%})%>
          </div>
          <a class="bottom-link" href="/">홈으로</a>
          <a class="bottom-link" href="/user/logout">로그아웃</a>
        </div>
      </div>
    </div>
  </body>
  <script src="/script/main.js"></script>
</html>
